# Settings Module Components (`/src/components/settings/`)

Handles all credentials inputs, local database checkpoints, and markdown styling preferences.

## Components

- `TokenConfiguration.tsx`: Key management panel for host URLs, OpenAI compatible secret keys, and offline connection checkers.
- `ConfirmationChecklist.tsx`: Secure checklist of sandboxed tools that require explicit manual verification before being run by autonomous agents.
