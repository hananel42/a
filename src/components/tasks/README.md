# Tasks UI Components Directory (`/src/components/tasks/`)

Contains visual task visualization components for rendering multi-agent task trees and delegation flows.

## Component Registry

- `TaskTreeVisualizer.tsx`: Interactive flowchart component rendering active task status nodes, sub-agent dependencies, execution progress, and step outcomes.
- `index.ts`: Barrel export exporting task visualization components.
